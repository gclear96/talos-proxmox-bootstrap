# Terraform
**/.terraform/
**/terraform.tfstate
**/terraform.tfstate.backup
**/terraform.tfstate.*.backup
**/terraform.tfstate.d/
**/*.tfvars
**/*.tfvars.json
**/*.tfplan
**/planfile

# Allow committing a non-secret cluster config for CI.
!terraform/cluster.auto.tfvars

# Tooling caches
.helm/

# Local outputs (kubeconfig, talosconfig)
out/*
!out/.gitkeep
*.kubeconfig
*.talosconfig

# OS
.DS_Store
